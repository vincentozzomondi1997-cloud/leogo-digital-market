name: LEOGO archived service marketplace workflow

# Historical workflow retained only as a valid, manually runnable placeholder.
# The service marketplace integration is already in the main application.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived_notice:
    runs-on: ubuntu-latest
    steps:
      - name: Archived workflow
        run: echo "Service marketplace integration is already complete; no action required."