name: Fix LEOGO admin order navigation

# Retained for historical reference only. The order-navigation fix is already
# integrated into the Admin Control Center. Do not run automatically on pushes.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived_notice:
    runs-on: ubuntu-latest
    steps:
      - name: Archived workflow
        run: echo "The Admin order-navigation fix is already integrated; no patch is required."