/* LEOGO PREMIUM DISCOVERY - approved profile catalogue. Cache-refresh marker 2026-09-12. */
(function(){
  if(window.__leogoPremiumDiscoveryInstalled)return;
  window.__leogoPremiumDiscoveryInstalled=true;
  const URL='https://twpiloiiigdghwcdjbnj.supabase.co',KEY='sb_publishable_c4iJwLdRuH85e0XuFnkSjg_mdxLN2fX',sb=window.supabase.createClient(URL,KEY);
  const esc=v=>String(v??'').replace(/[&<>\'\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','\"':'&quot;'}[c]));
  const publicPhoto=path=>path?sb.storage.from('premium-profile-public').getPublicUrl(path).data?.publicUrl||'':'';
  async function openDiscovery(){const q=await sb.rpc('get_premium_profiles_public');if(q.error){console.error('LEOGO Premium marketplace RPC:',q.error);alert(q.error.message||'Premium profiles are temporarily unavailable.');return}showCatalogue(Array.isArray(q.data)?q.data:[])}
  function requestProfile(id,type){
    const run=()=>window.LEOGOPremiumAcceptance?.request?.(id,type);
    if(typeof window.LEOGOPremiumAcceptance?.request==='function'){run();return}
    let tries=0;const wait=setInterval(()=>{tries++;if(typeof window.LEOGOPremiumAcceptance?.request==='function'){clearInterval(wait);run()}else if(tries>=30){clearInterval(wait);alert('Premium request service is still loading. Please try again.') }},100);
  }
  function showCatalogue(rows){document.getElementById('leogoPremiumDiscoveryModal')?.remove();const m=document.createElement('div');m.id='leogoPremiumDiscoveryModal';m.className='modal';const cards=rows.length?rows.map(p=>{const photo=publicPhoto(p.profile_picture_path);return '<div class="panel" style="margin:0;overflow:hidden"><div style="display:flex;justify-content:center;margin:-6px -6px 14px">'+(photo?'<img src="'+esc(photo)+'" alt="'+esc(p.username||'Premium profile')+'" loading="lazy" style="width:150px;height:150px;border-radius:50%;object-fit:cover;border:3px solid #ff7a00;background:#f4f4f4">':'<div style="width:150px;height:150px;border-radius:50%;display:flex;align-items:center;justify-content:center;background:#f4f4f4;font-size:48px">👤</div>')+'</div><div style="display:flex;justify-content:space-between;gap:10px;align-items:flex-start"><div><h3 style="margin:0 0 5px">'+esc(p.username)+'</h3><div class="muted" style="font-size:13px">'+esc(p.age||'')+(p.sex?' • '+esc(p.sex):'')+'</div><div class="muted" style="font-size:13px;margin-top:3px">📍 '+esc(p.location||'Location not specified')+'</div></div>'+(p.verified?'<span class="pill green">✓ VERIFIED</span>':'')+'</div>'+(p.orientation?'<div style="margin-top:10px;font-size:13px"><b>Orientation:</b> '+esc(p.orientation)+'</div>':'')+(p.availability?'<div style="margin-top:6px;font-size:13px"><b>Availability:</b> '+esc(p.availability)+'</div>':'')+(p.description?'<div style="margin-top:8px;font-size:13px;line-height:1.45">'+esc(p.description)+'</div>':'')+(p.fee!==null&&p.fee!==undefined?'<div style="margin-top:10px;font-weight:900">KSh '+Number(p.fee).toLocaleString()+'</div>':'')+'<div style="display:flex;gap:8px;flex-wrap:wrap;margin-top:12px"><button class="btn orange lpInterestBtn" data-id="'+esc(p.id)+'">EXPRESS INTEREST</button><button class="btn light lpBookingBtn" data-id="'+esc(p.id)+'">REQUEST BOOKING</button></div></div>'}).join(''):'<div class="empty">No approved Premium profiles are available yet. Check again later.</div>';m.innerHTML='<div class="modal-card" style="width:min(850px,100%)"><div class="modal-head"><div><div style="font-size:11px;font-weight:900;color:#ff7a00">LEOGO PREMIUM</div><h2 style="margin:3px 0">Find Premium Members</h2></div><button class="close" id="lpdClose">✕</button></div><div class="notice success">Only approved profiles are shown. Sensitive identity information is protected.</div><div id="lpdGrid" style="display:grid;grid-template-columns:repeat(auto-fit,minmax(260px,1fr));gap:14px">'+cards+'</div><div class="muted" style="font-size:12px;margin-top:15px">LEOGO facilitates the connection. Members decide whether to communicate or meet and remain responsible for their own safety and decisions.</div></div>';document.body.appendChild(m);document.getElementById('lpdClose').onclick=()=>m.remove();m.onclick=e=>{if(e.target===m)m.remove()};m.querySelectorAll('.lpInterestBtn').forEach(b=>b.onclick=()=>requestProfile(b.dataset.id,'interest'));m.querySelectorAll('.lpBookingBtn').forEach(b=>b.onclick=()=>requestProfile(b.dataset.id,'booking'));}
  function injectButton(){const modal=document.getElementById('leogoPremiumModal');if(!modal||modal.dataset.discoveryButton==='1'||!/Private Premium Area|Access confirmed/i.test(modal.textContent||''))return;const area=Array.from(modal.querySelectorAll('.panel')).find(x=>/Private Premium Area/i.test(x.textContent||''));if(!area)return;modal.dataset.discoveryButton='1';const wrap=document.createElement('div');wrap.style.cssText='display:flex;justify-content:flex-end;gap:10px;flex-wrap:wrap;margin-top:12px';wrap.innerHTML='<button class="btn primary" type="button" id="leogoPremiumDiscoverBtn">🔎 BROWSE PREMIUM PROFILES</button>';area.appendChild(wrap);wrap.querySelector('button').onclick=openDiscovery}
  function loadAcceptanceModule(){if(document.querySelector('script[data-leogo-premium-acceptance]'))return;const s=document.createElement('script');s.src='premium_acceptance_customer.js?v=20260912';s.dataset.leogoPremiumAcceptance='1';s.async=true;s.onload=()=>{window.LEOGOPremiumAcceptanceLoaded=true};document.head.appendChild(s)}
  const observer=new MutationObserver(injectButton);function start(){loadAcceptanceModule();observer.observe(document.body,{childList:true,subtree:true});injectButton()}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start);else start();
})();
